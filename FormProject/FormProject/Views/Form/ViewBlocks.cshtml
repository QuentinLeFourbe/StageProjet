@model FormProject.Models.Form

@{
    foreach (var block in Model.Block)
    {
        <div class="row blockModel" id="block">
            <div class="col-sm-10 ">
                <div class="panel panel-default">
                    @Html.Partial("ViewQuestions")
                </div>

            </div>
        </div>

    }
}